---
import GlobalFooter from '../../components/GlobalFooter.astro';
import { getCollection } from 'astro:content';

const allTemplates = await getCollection('templates');

// Sort by pubDate descending
allTemplates.sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());

// Normalise profession casing inconsistencies
function normaliseProfession(p: string): string {
  const map: Record<string, string> = {
    'Marketing Manager': 'Marketing Managers',
    'Project Management': 'Project Managers',
  };
  return map[p] ?? p;
}

// Group by profession
const professionOrder = ['HR Managers', 'Marketing Managers', 'Operations', 'Sales', 'Project Managers', 'Customer Service'];
const grouped: Record<string, typeof allTemplates> = {};
for (const t of allTemplates) {
  const prof = normaliseProfession(t.data.profession);
  if (!grouped[prof]) grouped[prof] = [];
  grouped[prof].push(t);
}

const orderedProfessions = [
  ...professionOrder.filter(p => grouped[p]),
  ...Object.keys(grouped).filter(p => !professionOrder.includes(p)),
];

const professionMeta: Record<string, { icon: string; hub: string }> = {
  'HR Managers':        { icon: '👥', hub: '/hr-managers' },
  'Marketing Managers': { icon: '📣', hub: '/marketing-managers' },
  'Marketing Manager':  { icon: '📣', hub: '/marketing-managers' },
  'Operations':         { icon: '⚙️', hub: '/operations-managers' },
  'Sales':              { icon: '💼', hub: '/sales-reps' },
  'Project Managers':   { icon: '📋', hub: '/project-managers' },
  'Project Management': { icon: '📋', hub: '/project-managers' },
  'Customer Service':   { icon: '🎧', hub: '/customer-service' },
  'Finance':            { icon: '💰', hub: '/finance' },
  'Legal':              { icon: '⚖️', hub: '/legal' },
  'Product Management': { icon: '🚀', hub: '/product-management' },
  'Real Estate':        { icon: '🏠', hub: '/real-estate' },
  'Teachers':           { icon: '📚', hub: '/teachers' },
};
const fallbackMeta = { icon: '📝', hub: '/' };

const toolIcons: Record<string, string> = {
  'Notion':   '📝',
  'Airtable': '🗃',
  'Google Sheets': '📊',
};
---
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Notion Templates — GPT Prompt Engine</title>
<meta name="description" content="Free Notion and Airtable templates organised by profession. Duplicate into your workspace in one click." />
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Carlito:ital,wght@0,400;0,700;1,400;1,700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
<link rel="sitemap" href="/sitemap-index.xml" />
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
:root {
  --bg: #FFFFFF; --bg-soft: #F2F2F2; --bg-dark: #0C1D5B; --bg-deeper: #0A1645;
  --text: #0C1D5B; --text-soft: #6B7699; --text-muted: #8B93AB;
  --rule: #E5E8F0; --rule-strong: #C8CEDD;
  --blue: #0C1D5B; --blue-tint: #EAEDF5;
  --green: #CDE954; --green-deep: #B5D344;
  --display: 'Carlito', 'Calibri', sans-serif;  --body: 'Carlito', 'Calibri', sans-serif;
  --mono: 'JetBrains Mono', monospace;
}
body { font-family:var(--body); background:var(--bg); color:var(--text); -webkit-font-smoothing:antialiased; line-height:1.6; }
a { color:inherit; text-decoration:none; }
.nav { position:sticky; top:0; z-index:100; background:var(--bg); border-bottom:1px solid var(--rule); padding:0 48px; display:flex; align-items:center; justify-content:space-between; height:64px; }
.nav-logo { font-family:var(--display); font-size:21px; font-weight:500; font-variation-settings:"opsz" 144; letter-spacing:-0.02em; color:var(--blue); display:flex; align-items:center; gap:7px; }
.nav-dot { width:8px; height:8px; border-radius:50%; background:var(--green); display:inline-block; animation:blink 2.5s ease-in-out infinite; }
@keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.35} }
.nav-center { display:flex; gap:4px; background:var(--bg-soft); padding:4px; border-radius:8px; }
.nav-link { padding:8px 16px; font-size:13px; font-weight:500; color:var(--text-soft); border-radius:5px; transition:all 0.15s; }
.nav-link:hover { color:var(--blue); background:rgba(12,29,91,0.04); }
.nav-link.active { background:var(--bg); color:var(--blue); font-weight:700; box-shadow:0 1px 3px rgba(12,29,91,0.08); }
.nav-cta { background:var(--green); color:var(--blue); padding:9px 20px; font-size:13px; font-weight:700; border-radius:6px; letter-spacing:0.01em; transition:all 0.15s; }
.nav-cta:hover { background:var(--green-deep); transform:translateY(-1px); }
.page-header { background:var(--bg-dark); padding:56px 48px 48px; border-bottom:3px solid var(--green); }
.page-header-inner { max-width:1100px; margin:0 auto; }
.page-header-crumb { font-size:11px; font-weight:700; letter-spacing:0.14em; text-transform:uppercase; color:rgba(255,255,255,0.4); margin-bottom:20px; display:flex; align-items:center; gap:8px; }
.page-header-crumb a { color:rgba(255,255,255,0.4); transition:color 0.15s; }
.page-header-crumb a:hover { color:var(--green); }
.page-header-crumb span { color:rgba(255,255,255,0.2); }
.page-header h1 { font-family:var(--display); font-weight:500; font-variation-settings:"opsz" 144; font-size:clamp(32px, 4vw, 48px); line-height:1.08; letter-spacing:-0.025em; color:white; margin-bottom:14px; }
.page-header h1 em { font-style:italic; color:var(--green); }
.page-header .lede { font-size:15px; color:rgba(255,255,255,0.55); line-height:1.65; max-width:560px; font-weight:400; }
.page-header-meta { display:flex; align-items:center; gap:24px; margin-top:28px; padding-top:24px; border-top:1px solid rgba(255,255,255,0.1); }
.meta-stat-num { font-family:var(--mono); font-size:20px; font-weight:600; color:white; line-height:1; margin-bottom:3px; }
.meta-stat-label { font-size:11px; font-weight:700; letter-spacing:0.1em; text-transform:uppercase; color:rgba(255,255,255,0.35); }
.meta-divider { width:1px; height:32px; background:rgba(255,255,255,0.1); }
.tool-toggle { background:var(--bg-soft); border-bottom:1px solid var(--rule); padding:0 48px; }
.tool-toggle-inner { max-width:1100px; margin:0 auto; display:flex; align-items:center; gap:12px; height:52px; }
.tool-toggle-label { font-size:12px; font-weight:700; letter-spacing:0.08em; text-transform:uppercase; color:var(--text-muted); margin-right:4px; }
.tool-btn { display:inline-flex; align-items:center; gap:7px; font-size:12px; font-weight:700; letter-spacing:0.04em; padding:6px 14px; border-radius:6px; cursor:pointer; transition:all 0.15s; border:1.5px solid var(--rule); color:var(--text-soft); background:var(--bg); font-family:var(--body); }
.tool-btn:hover { border-color:var(--blue); color:var(--blue); }
.tool-btn.active { background:var(--blue); color:white; border-color:var(--blue); }
.tool-btn-icon { font-size:14px; }
.main { max-width:1100px; margin:0 auto; padding:40px 48px 80px; }
.profession-group { margin-bottom:56px; }
.profession-group[data-hidden] { display:none; }
.group-header { display:flex; align-items:center; gap:14px; margin-bottom:20px; padding-bottom:16px; border-bottom:1px solid var(--rule); }
.group-icon { width:36px; height:36px; background:var(--bg-dark); color:var(--green); border-radius:7px; display:flex; align-items:center; justify-content:center; font-size:16px; }
.group-title { font-size:18px; font-weight:700; color:var(--blue); }
.group-count { margin-left:auto; font-family:var(--mono); font-size:11px; font-weight:600; color:var(--blue); background:var(--blue-tint); padding:4px 12px; border-radius:999px; }
.group-link { font-size:12px; font-weight:700; letter-spacing:0.06em; text-transform:uppercase; color:var(--text-muted); transition:color 0.15s; display:flex; align-items:center; gap:5px; }
.group-link:hover { color:var(--blue); }
.cards-grid { display:grid; grid-template-columns:repeat(auto-fill, minmax(300px, 1fr)); gap:14px; }
.card { background:var(--bg); border:1px solid var(--rule); border-radius:10px; overflow:hidden; display:block; transition:all 0.2s; }
.card:hover { border-color:var(--rule-strong); box-shadow:0 6px 20px rgba(12,29,91,0.07); transform:translateY(-1px); }
.card-head { display:flex; align-items:center; gap:10px; padding:12px 16px; background:var(--bg-soft); border-bottom:1px solid var(--rule); }
.chip { display:inline-flex; align-items:center; gap:5px; font-size:11px; font-weight:700; letter-spacing:0.08em; text-transform:uppercase; padding:4px 10px; border-radius:999px; background:var(--blue-tint); color:var(--blue); }
.chip.teal { background:#E8F8F4; color:#0A6B50; }
.chip-dot { width:5px; height:5px; border-radius:50%; background:currentColor; flex-shrink:0; }
.card-tool-badge { margin-left:auto; display:inline-flex; align-items:center; gap:5px; font-family:var(--mono); font-size:10px; font-weight:600; color:var(--text-muted); background:var(--bg); border:1px solid var(--rule); padding:2px 8px; border-radius:4px; }
.card-body { padding:14px 16px; }
.card-title { font-size:14px; font-weight:700; color:var(--blue); line-height:1.35; margin-bottom:6px; }
.card-desc { font-size:12px; color:var(--text-soft); line-height:1.6; }
.card-footer { display:flex; align-items:center; justify-content:space-between; padding:10px 16px; border-top:1px solid var(--rule); background:var(--bg-soft); }
.card-tags { display:flex; gap:6px; flex-wrap:wrap; }
.card-tag { font-size:10px; font-weight:600; color:var(--text-muted); background:var(--bg); border:1px solid var(--rule); padding:2px 8px; border-radius:4px; }
.card-duplicate { display:inline-flex; align-items:center; gap:5px; font-size:11px; font-weight:700; letter-spacing:0.04em; text-transform:uppercase; color:var(--blue); transition:color 0.15s; }
.card-duplicate:hover { color:var(--green-deep); }
.card-duplicate-icon { width:22px; height:22px; background:var(--green); color:var(--blue); border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:10px; }
footer { background:var(--bg-deeper); border-top:1px solid rgba(255,255,255,0.06); padding:28px 48px; }
.footer-inner { max-width:1100px; margin:0 auto; display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:16px; }
.footer-logo { font-family:var(--display); font-size:16px; font-weight:500; font-variation-settings:"opsz" 144; color:rgba(255,255,255,0.45); display:flex; align-items:center; gap:6px; }
.footer-logo-dot { width:6px; height:6px; border-radius:50%; background:var(--green); }
.footer-links { display:flex; gap:24px; list-style:none; }
.footer-links a { font-size:12px; font-weight:600; letter-spacing:0.08em; text-transform:uppercase; color:rgba(255,255,255,0.25); transition:color 0.15s; }
.footer-links a:hover { color:rgba(255,255,255,0.7); }
.footer-copy { font-family:var(--mono); font-size:11px; color:rgba(255,255,255,0.2); }
@media (max-width:960px) {
  nav,.page-header,.tool-toggle,.main,footer { padding-left:20px; padding-right:20px; }
  .cards-grid { grid-template-columns:1fr; }
}
/* MOBILE NAV */
.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  padding: 4px;
  background: none;
  border: none;
}
.nav-hamburger span {
  display: block;
  width: 22px;
  height: 2px;
  background: var(--blue);
  border-radius: 2px;
  transition: all 0.25s;
}
.nav-mobile-menu {
  display: none;
  position: fixed;
  top: 64px;
  left: 0;
  right: 0;
  background: white;
  border-bottom: 1px solid var(--rule);
  padding: 16px 20px;
  z-index: 99;
  flex-direction: column;
  gap: 4px;
}
.nav-mobile-menu.open { display: flex; }
.nav-mobile-menu a {
  padding: 12px 16px;
  font-size: 15px;
  font-weight: 500;
  color: var(--blue);
  border-radius: 8px;
}
.nav-mobile-menu a:hover { background: var(--bg-soft); }
.nav-mobile-menu .mob-cta {
  margin-top: 8px;
  background: var(--green);
  color: var(--blue);
  font-weight: 700;
  text-align: center;
}

@media (max-width: 768px) {
  .nav-center { display: none; }
  .nav-cta { display: none; }
  .nav-hamburger { display: flex; }
}
</style>
</head>
<body>

<nav class="nav">
<div class="nav-mobile-menu" id="mobileMenu">
  <a href="/prompts">Prompts</a>
  <a href="/workflows">Workflows</a>
  <a href="/templates">Templates</a>
  <a href="/hr-managers" class="mob-cta">Browse Free</a>
</div>
  <a href="/" class="nav-logo"><span class="nav-dot"></span>GPT Prompt Engine</a>
  <div class="nav-center">
    <a href="/prompts" class="nav-link">Prompts</a>
    <a href="/workflows" class="nav-link">Workflows</a>
    <a href="/templates" class="nav-link active">Templates</a>
  </div>
  <a href="/hr-managers" class="nav-cta">Browse Free</a>
  <button class="nav-hamburger" id="hamburger" aria-label="Menu">
  <span></span><span></span><span></span>
</button>
</nav>

<div class="page-header">
  <div class="page-header-inner">
    <div class="page-header-crumb"><a href="/">Home</a><span>/</span>Templates</div>
    <h1>Templates built for<br/><em>your specific role.</em></h1>
    <p class="lede">Free Notion and Airtable templates organised by job title. Duplicate into your workspace in one click. Premium bundles available on Gumroad.</p>
    <div class="page-header-meta">
      <div class="meta-stat">
        <div class="meta-stat-num">{allTemplates.length}</div>
        <div class="meta-stat-label">{allTemplates.length === 1 ? 'Template' : 'Templates'} live</div>
      </div>
      <div class="meta-divider"></div>
      <div class="meta-stat">
        <div class="meta-stat-num">Notion</div>
        <div class="meta-stat-label">+ Airtable</div>
      </div>
      <div class="meta-divider"></div>
      <div class="meta-stat">
        <div class="meta-stat-num">1-click</div>
        <div class="meta-stat-label">Duplicate</div>
      </div>
    </div>
  </div>
</div>

<div class="tool-toggle">
  <div class="tool-toggle-inner">
    <span class="tool-toggle-label">Filter by tool</span>
    <button class="tool-btn active" data-tool="all"><span class="tool-btn-icon">✦</span>All</button>
    <button class="tool-btn" data-tool="Notion"><span class="tool-btn-icon">📝</span>Notion</button>
    <button class="tool-btn" data-tool="Airtable"><span class="tool-btn-icon">🗃</span>Airtable</button>
    <button class="tool-btn" data-tool="Google Sheets"><span class="tool-btn-icon">📊</span>Google Sheets</button>
  </div>
</div>

<main class="main">
  {orderedProfessions.map(profession => {
    const templates = grouped[profession];
    const meta = professionMeta[profession] ?? fallbackMeta;
    const count = templates.length;
    return (
      <div class="profession-group" data-profession={profession}>
        <div class="group-header">
          <div class="group-icon">{meta.icon}</div>
          <div class="group-title">{profession}</div>
          <span class="group-count">{count} {count === 1 ? 'template' : 'templates'}</span>
          <a href={meta.hub} class="group-link">View hub →</a>
        </div>
        <div class="cards-grid">
          {templates.map(t => {
            const toolIcon = toolIcons[t.data.tool] ?? '📄';
            return (
              <a href={`/templates/${t.id}`} class="card" data-tool={t.data.tool}>
                <div class="card-head">
                  <span class="chip teal"><span class="chip-dot"></span>Template</span>
                  <span class="card-tool-badge">{toolIcon} {t.data.tool}</span>
                </div>
                <div class="card-body">
                  <div class="card-title">{t.data.title}</div>
                  <div class="card-desc">{t.data.description}</div>
                </div>
                <div class="card-footer">
                  <div class="card-tags">
                    {t.data.tags.slice(0, 3).map((tag: string) => (
                      <span class="card-tag">{tag}</span>
                    ))}
                  </div>
                  <span class="card-duplicate"><span class="card-duplicate-icon">⧉</span>Duplicate free</span>
                </div>
              </a>
            );
          })}
        </div>
      </div>
    );
  })}
</main>



<script is:inline>
  const buttons = document.querySelectorAll('.tool-btn');
  const cards = document.querySelectorAll('.card[data-tool]');
  const groups = document.querySelectorAll('.profession-group[data-profession]');

  buttons.forEach(btn => {
    btn.addEventListener('click', () => {
      buttons.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');

      const selected = btn.getAttribute('data-tool');

      cards.forEach(card => {
        if (selected === 'all' || card.getAttribute('data-tool') === selected) {
          card.style.display = '';
        } else {
          card.style.display = 'none';
        }
      });

      // Hide groups where all cards are hidden
      groups.forEach(group => {
        const visible = group.querySelectorAll('.card[data-tool]:not([style*="display: none"])');
        group.style.display = visible.length === 0 ? 'none' : '';
      });
    });
  });
</script>
<script is:inline>
  document.getElementById('hamburger').addEventListener('click', () => {
    document.getElementById('mobileMenu').classList.toggle('open');
  });
</script>
<GlobalFooter />
</body>
</html>
